describe('auth skeleton', () => {
  it('should sign up a user (skeleton)', () => {
    expect(true).toBe(true);
  });
  it('should login user (skeleton)', () => {
    expect(true).toBe(true);
  });
});


